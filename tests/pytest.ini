[pytest]
asyncio_mode = auto
testpaths = tests/unit tests/integration
markers =
    integration: requires running Docker Compose stack (docker compose up -d)
addopts =
    -v
    --tb=short
    --strict-markers
