# Minimal dev config for Mosquitto.
# NO AUTH — fine inside the compose network, NOT fine for production.

listener 1883
allow_anonymous true

# Don't persist messages to disk in dev
persistence false

# Log to stdout so `docker compose logs mosquitto` shows what's happening
log_dest stdout
log_type error
log_type warning
log_type notice
log_type information
