# PRD-0003 Phase 1.4 實作計畫 — Human-Review / MCP / Observability / Audit

| 欄位 | 內容 |
|------|------|
| 對應 PRD | [PRD-0003](PRD-0003-Device-Registry-Auto-Discovery.md) §12.4 / §8.4 / §10 / §8.7 |
| 決策紀錄 | [decision-log](../governance/decision-log.md) DL-012 |
| 狀態 | Planned（2026-05-29） |
| 前置 | Phase 1.1+1.2+1.3 已合併 dev（merge 38581f1） |

> 記錄文件，不重述 spec；衝突以 PRD-0003 為準。

## ⚠️ 從 Phase 1.3 帶過來的 MUST-DO（非可選 — 旁路監控確認）

這兩項在 Phase 1.3 是**實質 deferred**，必須在 Phase 1.4 補完，且**接真 provider 上線前為硬前置**：

1. **FR-329 budget HARD CAP — implemented (5e0b86d) + reservation-leak fix (2665012) + §13 tests**
   - **Done**: pre-call estimated-cost **reservation** (`budget_reserve`) under a **budget-namespace `pg_advisory_xact_lock`** (`budget:provider:period`, separate from §8.6.8 `device:` lock) → near-budget single call cannot cross; concurrent workers cannot both pass. **`budget_settle`** reconciles reservation→actual (refund; full refund on fallback). **Reservation never leaks**: `process_message` wraps reserve→classify→apply→settle in try/finally; an unexpected exception after a successful reservation refunds the full reservation. unpriced-model WARNING. §13 tests: near-budget denied, concurrent→exactly-one, settle refund, full refund, **exception-refund (no leak)**, real-provider settle. 185 tests, device_service ~99% (test container).
   - **Remaining caveats (NOT a production guarantee — real-provider enablement)**: NOT run against live Anthropic/OpenAI; cost is an estimate (not real billing); **L2 guardrail `provider='guardrail'` ledger row not yet recorded** (MockGuardrail is free); rare guardrail-post-block-after-L1 path refunds full reservation though L1 spent tokens (minor under-count); the in-`finally` refund itself is best-effort (suppressed on a secondary DB failure). (decision context: local DL-013/14/15 — NOT in git; this plan doc + commits are the tracked record, since `doc/governance/decision-log.md` is gitignored by convention.)
2. **MQTT subscriber reconnect / error branch 測試覆蓋**
   - 現況（保守表述）：`run_subscriber` 已加 defensive reconnect loop + lifespan task done-callback，但 **reconnect 路徑與 per-message error branch 尚無單元/整合測試**（commit 已註明 uncovered）。
   - Must-do：補測試 — (a) broker 斷線觸發 reconnect（可用 fake aiomqtt client 模擬 raise→重試）；(b) 單則訊息 `process_message` 拋例外不殺 loop；(c) done-callback 在 task 異常結束時記 error。

## Phase 1.4 其餘範圍（§12.4）

- ✅ **Human-review endpoint（DONE，batch 1）**：`GET /devices/{id}/human-review` 回 §8.4 digest（讀 `device_review_digests`，不現呼 LLM；system_fallback 亦回 200）。OPS channel；新 `repositories/digest_repo.py`（`get_with_device` 單次 LEFT JOIN 原子取 device-exists+digest，避開讀競態；`get` 為 MCP 預留的 digest-only 取用）；`models.DigestOut` response_model；404 分裝置不存在 / 無 digest。6 integration tests，touched 檔 100% cov（容器量測）。
- **`/ai-feedback` + correction loop（分 3 slice）**：
  - ✅ **2a（DONE）**：`correction_validator.py`（§7.3a 寫入檢查：NFKC→length 30-500→control char→**Cf format char 拒絕**[sec-review H-1/M-1：ZWSP/RTL-override 等不被 NFKC 收斂、可拆解 injection phrase，故一律拒絕]→structural `<>{}\\\``→secret 黑名單→injection phrase 白名單）+ `key_id.py`（`hash_key_id` HMAC-SHA256(salt, api_key)，不存原始 key，FR-345；fail-closed on empty salt/key/version）+ config `audit_hash_salt`(secret,.env)/`audit_salt_version`(TOML)。29→31 validator unit tests + config drift test 改由 SECRET_FIELDS 推導。security-review 通過後合併。
  - ✅ **2b（DONE — corrections 寫入/列出/失效 + 校驗 + 速率限制；FR-330 兩個 action flag 延 2c）**：`correction_repo.py` + OPS `POST /devices/{id}/ai-feedback`、`GET /devices/{id}/corrections`（`active_only` 可選）、`POST /devices/{id}/corrections/{cid}/deactivate`（FR-330 核心 / FR-341）。寫入前套 §7.3a validator（**內容違規回 400**，對齊 FR-330 驗收，非 422）；HMAC `created_by_key_id`（不存原始 key）+ `salt_version`；`prompt_version_at_correction` 由**伺服端 stamp**（`prompt.PROMPT_VERSION`，非 client 傳入）；deactivate body 欄位 `reason`（對齊 PRD §624）。`audit_hash_salt` 缺失：啟動期 WARNING（不硬擋開機，sec-review M-2）+ 寫入點 fail-closed 503（no DB row）。FR-343 per-key-id 30/h、per-device 10/h DB-count 速率限制回 **429 enforcement（done）**。**FR-330 `rerun_classification` / `demote_to_candidate`：加入 model 但 true 一律回 501（不靜默吞），實際行為與 classify/解凍路徑一起留 2c**（demote 會解凍 human 記錄、需 freeze override，本質屬 2c）。11 corrections integration tests；routes/devices.py + correction_repo.py + models.py 100% cov（容器量測，非 CI/prod 保證）。code-review 後合併。
    - **2b 補強（review 後，DONE）**：(1) **FR-343 metric 保守表述**——超限目前只發 `_audit.warning` 結構化 log line（interim 可被 log-based alert 消費）；規格要求的 `ai_feedback_rate_limited_total{key_id,device_id}` numeric counter **尚未做**（無 metrics registry；且 key_id=HMAC + device_id 作 label 屬高基數反模式）→ **真正 counter 歸 observability 批次**，2b 僅宣稱「429 enforcement done, alert metric pending」。(2) **per-key 改硬限制**——同 tx 內加 `pg_advisory_xact_lock('correction-key:'||key_id)`（device lock 後取，固定序不死鎖），跨 device 並發不再能繞過 per-key。(3) **migration 013** `device_corrections_key_time (created_by_key_id, created_at DESC)` 索引，補 per-key count 熱路徑。(4) **demote 收緊**：僅 `status=='confirmed'` 可 demote，retired/candidate 皆回 409（不對非 confirmed 清 classified_by）。(5) **OpenAPI**：`api/openapi.yml` 尚未含 /ai-feedback、/corrections——runtime API ahead of OpenAPI，§15.C 批次同步（其他 client/agent 讀 openapi 須知此漂移）。
  - ✅ **2c-retrieval（DONE — FR-331/332 + §8.6.5a + migration 012）**：`correction_context.py`（`build_context`、`device_type_family`[家族=自身，單點可擴充]、`topic_prefix`[前兩段、兩段皆需非空]、`cap_to_prompt_size`[32KB §8.6.5a，**二分搜尋**最大可容前綴，O(log n) render，遵 FR-331 無筆數上限]）+ `correction_repo` `retrieve_relevant`（device/gateway[JOIN devices]/type-family/topic-prefix 聯集，is_active only）/`latest_corrected_device_type`（device→gateway fallback，FR-332 來源）/`mark_applied`（applied_count+last_applied_at，與 apply_outcome 同 tx）。**migration 012**：device_service_ai 得 `device_corrections` SELECT + 欄位限定 UPDATE(applied_count,last_applied_at)（不含 INSERT/DELETE/內容欄位；DB column-priv 強制；user 授權套用 dev DB）。discovery.process_message 接線：retrieve→cap→注入 sanitized→classify(latest_correction_device_type)→summary_source=='llm' 才 mark_applied。code-review WARNING→修 O(n²)（改二分）/mark_applied 同 tx/topic_prefix 硬化/int 強轉後合併。21 corrections/retrieval/context + migration012(3) tests，correction_context+correction_repo 100% cov、discovery 95%。
  - ✅ **2c-flags（DONE — demote 實作；rerun 改歸 reclassify/MCP 批次）**：FR-330 `demote_to_candidate` 已實作 — `/ai-feedback` 帶 freeze-override token，寫 correction 後將 device confirmed→candidate（`device_repo.demote_to_candidate`：status=candidate、classified_by=NULL、confirmed_at=NULL），使 AI 路徑可重新分類；**retired 裝置拒絕 demote 回 409**（code-review HIGH：不可復活退役裝置）、demote 回 None→404（並發刪除防護）、audit log 帶 from_status+correction_id。`rerun_classification`（即時 classify）**仍回 501**：需「取既有裝置近 ≤20 筆原始量測樣本（OPS pool 讀 measurements，§234/§761）→ classify」的 on-demand reclassify pipeline，與 MCP `classify_with_context`（§671）同一原語 → **改歸下方 MCP server 批次**一起做，避免重複。code-review WARNING→修 2 個 HIGH（retired guard、None 檢查）後合併。269 device_service tests pass。（FR-344 大量 deactivate alert 仍歸 audit/observability 批次。）
- **`device_audit_log` 表 + FR-339/FR-344 告警**（分 slice；下一個真正 blocker，先於 Grafana/MCP）：
  - ✅ **Slice A（DONE）**：**migration 014** `device_audit_log`（**append-only**：AI+OPS 僅 INSERT/SELECT，無人 UPDATE/DELETE→防竄改；無 device_id FK→audit 比 device 長命；event_type/actor CHECK；FR-339 `(device_id,event_type,event_time)` + FR-344 `(actor_key_id,event_type,event_time)` 索引；已套 dev）+ `repositories/audit_repo.py`（`record` 在呼叫者 tx 內 append、`count_recent` 供 FR-339/FR-344 window）。override/reject 已改為**交易內** INSERT audit row（atomic，§554/§8.7.5）+ 保留 log line。272 device_service tests；audit_repo 100% cov。code-review 後合併。append-only 副作用：測試不能用 OPS pool 清 audit→改用 since=cutoff 斷言。
  - ✅ **Slice B（DONE）**：抽 `correction_service.py`（`CorrectionService` create_feedback/list_feedback/deactivate；`CorrectionServiceError(status,detail)` 領域例外，router 一行 `_call` map 成 HTTP，**service 不 import FastAPI**；`_key_identity` 必需 503 vs `_optional_identity` best-effort）。router 三個 handler 變薄委派（227 行；移除死碼 _validated_correction_text/_created_by_key_id/常數/未用 import）。補 audit row：**ai_feedback_create / demote**（成功路徑與 create 同 tx atomic）/ **deactivate**（同 tx）/ **rate_limit_exceeded**（**獨立 tx commit**——主 tx 內 record 後 raise 429 會被回滾抹掉，故拒絕路徑另開 tx 寫 audit 再 raise）。deactivate 帶 best-effort actor_key_id（salt 未設→NULL，不 503，FR-344 window 用）。274 device_service tests；correction_service 99% cov。code-review APPROVE（(not over_key) 短路為刻意優化非回歸）。
  - 🟡 **Slice C1（FR-344 偵測/標記 done；Telegram/Grafana alert query 待 Grafana 批次 → FR-344 尚未完整）**：`CorrectionService.deactivate` 加 per-key sliding window 偵測（1h≥5 或 24h≥20，常數 `DEACTIVATE_ALERT_1H/24H` + 純函式 `mass_deactivate_suspicious`）。**append-only** 不能事後 UPDATE→寫入前 count+1，跨閾值把 `detail.suspicious/count_1h/count_24h` 標進 deactivate audit row + log warning。**並發準確性（review #2）**：deactivate 已補 per-key `correction-key:` advisory lock（同 create_feedback），同 key 並發不會漏標跨閾值那筆——security-grade，非 heuristic。key_id=None（無 salt）跳過。**FR-344 的「Telegram alert」未做**：由 Grafana 對 device_audit_log 跑 window SQL 出（低基數 event_type 維度，非 key/device label），歸 Grafana 批次。故 PRD FR-344 = 偵測+audit 標記 done、alert 投遞 pending。service 99% cov。code-review APPROVE。
  - 🟡 **Slice C2（L2 guardrail BLOCK audit done + least-privilege；FR-339 連續 BLOCK alert query 待 Grafana）**：classifier 在 pre/post BLOCK 時建 `GuardrailBlock`（phase、threat_category、reasoning[截斷 200]、`l1_input_hash`=sha256(rendered prompt)、`l1_output_hash`=L1 結果指紋僅 post 有）→ 帶進 `Outcome.guardrail_block`（BLOCK fallback 不入 cache）。discovery 在 apply_outcome 同 ai_tx 內寫 `guardrail_block` audit row（actor=ai，原始 prompt **不**存、只存 hash）。**migration 015 least-privilege（review #4）**：AI 對 device_audit_log 改 column-scoped `SELECT(id)` only（保 INSERT…RETURNING id 可用）+ 撤表級 SELECT→AI 讀不到 detail/actor_key_id 等內容欄位；OPS 不變。已套 dev。security-review APPROVE（0 CRIT/HIGH；reasoning 截斷防未來 LLM guardrail echo payload）。282 tests。**FR-339 的「連續 5 次/1h alert」未做**：同 FR-344，由 Grafana 對 guardrail_block rows 跑 per-device window SQL 出。
  - **低基數 observability 出口（沿用）**：rate_limit_exceeded / deactivate(detail.suspicious) / guardrail_block 皆 device_audit_log row；Grafana 以 event_type 維度低基數 window COUNT 告警（FR-339/FR-344 Telegram），不把 key_id/device_id 當 Prometheus label。
  - 🟢 **FR-339/FR-344 Grafana alert rules（DONE，本片）**：`infra/grafana/provisioning/alerting/rules.yaml` 新增 Group「EMS-設備分類安全」三條 alert（共用 TimescaleDB datasource + Telegram contact point）：`ems-guardrail-block-burst`（FR-339：guardrail_block 同 device 1h≥5）、`ems-mass-deactivate-1h`（FR-344：deactivate 同 key 1h≥5）、`ems-mass-deactivate-24h`（24h≥20）。**低基數**：SQL `GROUP BY device_id/actor_key_id HAVING count(*)>=N` 取「跨閾值數量」單一指標，device_id/key_id 不進 Prometheus label。YAML + 三條 SQL 已對 dev DB 驗證可執行；**rules 於 Grafana 重啟/provisioning reload 後生效**（本片未重啟共享 ems-grafana，未線上驗載入）。→ **至此 audit event source 完整 + FR-339/FR-344 告警 query 已備；尚缺：線上載入驗證 + 4 panels（下方 Grafana panel）。**
  - **reasoning 欄位決策（review 回應）**：PRD §8.7.5 明列 `reasoning` 為必要欄位，**保留不刪**（刪需 ADR）。現 MockGuardrail 的 reasoning 僅回顯**我方固定 marker 名**（如 `injection marker: ignore previous`，marker 取自固定 _INJECTION 集，非攻擊者任意文字）+ 截斷 200 + 僅 OPS 可讀；sec-review 已接受。**carried caveat**：未來改用 LLM 型 GuardrailProvider 時，必須在 reasoning 進 audit 前 strip/bound 任何逐字攻擊內容（enum/code 化或長度+內容過濾）。
- **MCP server / on-demand reclassify**（分 slice）：
  - ✅ **Slice 1a（DONE — reclassify 原語 + REST rerun_classification）**：`repositories/measurements_repo.py`（`recent_samples` 取近 ≤20 筆，table 固定 allowlist 防注入；`table_for_gateway` ems→electricity / kc→factory）+ `discovery_pipeline.reclassify_device`（OPS pool 讀 device routing + measurements[AI 無 SELECT 權，§234] → 複用 load_correction_context/classify_under_budget，`force=True` 強制 cache miss）。`load_correction_context` 由 `fields:dict` 泛化為 `samples:list[dict]`（discovery 傳 `[fields]`，行為不變）。**REST `rerun_classification` 不再 501**：correction commit 後觸發 reclassify（best-effort，try/except+WARNING log，失敗不回滾 correction）。reclassify 仍過 sanitizer/guardrail/budget/freeze 四層；frozen 裝置 apply_outcome no-op（不繞凍結）。security-review 0 CRIT/HIGH（SQL 注入/role-pool/freeze/force-cache 全 clean；1 LOW=改 suppress 為 log-then-swallow）。288 tests，measurements_repo 100%、discovery_pipeline 97%。
  - ✅ **Slice 1a 補強（review 後）**：(1) reclassify 加 **candidate-only 守衛**——非 candidate（confirmed/retired）在讀樣本/呼叫 LLM 前就 return None+log，省 budget（apply_outcome 本就會 no-op）；rerun confirmed device 須先 demote。(2) rerun 結果 **route log 透明化**（None=no-op / 已重分類 status）。(3) **FR-339/FR-344 alert 排除 `device_id LIKE 'itest-%'`**（+FR-344 `device_id IS NOT NULL`）→ 整合測試在 append-only audit table 累積的 rows 不再誤觸共享 dev 告警（三條 SQL 重驗皆 0.0）。(4) 清掉 alerting dir 的 `.swp/.swo` 編輯器暫存檔。FR-339 採 window-count（爆量）非嚴格 consecutive（對齊 PRD 驗收，已於 rule 註解載明；嚴格 consecutive 走 ADR）。code-review APPROVE（1 MEDIUM=prod SQL 內 'itest-' 前綴 documented-acceptable；1 LOW=`_su()` 測試 helper 兩檔重複 → 待抽 conftest follow-up）。290 tests。
  - ✅ **Slice 1b-i（DONE — MCP tool 函式，transport-agnostic）**：`mcp_tools.py` 三個 AI 通道 tool（ADR-012/§8.2）：`list_low_confidence_candidates`（candidate 且 ai_confidence≤0.9 的 digest，`digest_repo.list_low_confidence` JOIN devices+digests，AI pool 讀）/`get_device_digest`（複用 `digest_repo.get`，缺→ToolError not_found）/`classify_with_context`（驗 hint→`reclassify_device(hint=)` force cache-miss）。`reclassify_device` 加 `hint` 參數（驗過的 hint 以合成最新 CorrectionContext 注入 prompt，不進 applied_ids）。`validate_correction_text` 參數化 min_len/max_len（hint 用 1-500，仍跑全部 §7.3a 注入/結構/secret/format 檢查）。security-review CLEAN（0 CRIT/HIGH；hint 注入有 validator+L2 guardrail 雙層；只能重分類 candidate 不繞凍結；list query 參數化+AI 權限 OK）。10 MCP/reclassify tests，mcp_tools 100% cov。295 device_service tests。
  - ✅ **Slice 1b-ii（DONE — MCP Streamable-HTTP server）**：`mcp_server.py` 用**官方 `mcp` SDK（FastMCP，已驗 py3.11 可跑，無須升 3.12）**——`stateless_http=True, json_response=True`，lifespan 建 db+classifier，三 tool 包裝 mcp_tools + 每 call audit（tool/caller_ip/device/status/latency_ms）。`AuthRateLimitMiddleware`：X-API-Key=AI（**hmac.compare_digest** timing-safe、空 key fail-closed）+ per-IP 60/min；forward 非-HTTP scope 讓 inner lifespan 起。新 compose service `device-service-mcp`（同 image、command 跑 mcp_server:app:8766、`127.0.0.1:8766:8766` loopback-only host map、0.0.0.0 in-container[host map 需要]）。requirements 加 `mcp>=1.27,<2`。security-review 0 CRIT/HIGH（修 M-1 timing-safe key、M-3 caller_ip 入 audit；記錄 M-2 rate-limit 經 docker-proxy 實為 global / L-1 0.0.0.0-in-container / L-3 rotate 需 restart 之 trade-off）。4 e2e tests（401 無 key / 三 tool list / get happy+not_found / classify injection-hint→error）+ 6 mcp_tools。300 device_service tests。**=> MCP server 完整：AI 通道三 tool、無 confirm/override/reject、classify_with_context 仍過 sanitizer/guardrail/budget/candidate-guard。**
- **Grafana panel**：鎖定 4 個（pending count / status distribution / error & latency / cost）。
- **真實跨 provider L2 guardrail E2E**（§8.7.3，production 強制跨 provider，Promotion P-1）。
- **§15.C 文件同步**：`api/openapi.yml`（+ /ai-feedback、/corrections、/human-review）、`doc/operations/容器速查表.md`（容器數 12→**14**，加 ems-device-service + ems-device-service-mcp）、操作手冊（MCP endpoint 127.0.0.1:8766、AI key rotation 需 restart device-service-mcp） — 見 [[feedback_ems_doc_sync]]。

## 重構紀錄
- ✅ **discovery.py 解耦（review #5，DONE）**：`process_message` 原本混 admission/retrieval/cap/budget/classify/persist；抽出 `discovery_pipeline.py` 三段——`load_correction_context`（FR-331 retrieval + sanitize + §8.6.5a 32KB cap）/ `persist_outcome`（同 tx apply_outcome + applied_count + FR-339 guardrail audit）/ `classify_under_budget`（FR-329 reserve→classify→persist→settle→leak-safe finally）。`process_message` 留 admission + 編排。**純行為保留重構**（code-review 逐點驗證等價、無 import cycle、282 tests 不變全綠）。discovery.py 211→~100 行。
- ⏳ 其餘臃肿檔（test_migrations.py 600+ 行、test_device_service_corrections.py 350+ 行）為測試檔，低優先；待需要時再分檔。

## 流程（不變）
每批 TDD + **合併前 code review agent**（[[feedback_review_before_merge]]）；計畫/決策落 repo 記錄（[[feedback_plans_need_record_doc]]）；測試在 throwaway 容器跑（[[reference_ems_test_runtime]]）。

## 覆蓋率 / 測試的保守表述（沿用）
所報數字為 throwaway `python:3.11-slim` 容器內、device_service 模組之 pytest-cov 量測，**非 CI/production 全面保證**；整合測試在 DB/mosquitto 不可達時會 skip；`tests/Makefile` 的 coverage target 仍只量 simulator（待補 device-service target）。
