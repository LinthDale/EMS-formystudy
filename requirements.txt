# Root Python requirements for local EMS development and tests.
#
# This file is pip-installable:
#   python -m pip install -r requirements.txt
#
# Scope:
# - services/simulator local runtime
# - tests/ local test runner dependencies
#
# Not included:
# - Docker images and host tools; see requirements-inventory.md.
# - external/kc_modbus_mcp and external/kc_iot_gateway runtime environments.
#   They intentionally use their own pyproject.toml files and Python 3.12
#   container images. They also require pymodbus >=3.7, while the EMS simulator
#   pins pymodbus==3.6.9, so combining every component into one pip environment
#   would create a resolver conflict.

fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
pymodbus==3.6.9

pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27
psycopg2-binary>=2.9
paho-mqtt>=2.0
